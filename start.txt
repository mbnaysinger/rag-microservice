Neste projeto atual, quero simuluar um microsserviço para gerenciar bancos de dados vetoriais (seja onpremise ou cloud);
atualmente estou testando 4 opções, Mysql IA 9.4, PostgreSQL com pgvector, MongoDB com vector search e MariaDB com suporte a bancos vetoriais...
todos no formado onpremise.
Sendo assim, quero algo bem desenvolvido, distribuido e em padrão clean code.
para a parte de logs, quero usar o pino.
quero uma arquitetura hexagonal bem definida, com a cadama de API, DOMÍNIO e INFRAESTRUTA.
Sendo a camada de api com o rest, dtos e converters... camada de domínio com models, service e ports... camada de infraestrutar com adapter, repositories, entities e conveters... 
traga as melhores práticas de codificação e distribuição de módulos, neste primeiro momento não precisa se preocupar com autenticação.
neste projeto, observabilidade é a chave, pensem logs e implementações que facilitem futuras capturas por grafana loki ou apps correlatos...
utilize padrões de nomenclatura e nomes de variáveis em inglês...
estruture um readme bem explicativo e com on board amigável para novos desenvolvedores.
lembre de trabalhar por etapas, crie um TODO para que vá validando cada etapa a ser desenvolvida....
por favor, atente-se para:
- Todos os bancos tem suporte a tipagem VECTOR(n), estou usando atualmente 3072 pois é o formato que o text-embedding-003 da openia utiliza hoje...
mas preciso que garanta que os binários vector salvos tenham realmente o conteúdo dos chunks extraídos...
para a parte dos chunks, para leitura dos documentos, utilizei na implementação inicial o langchain, veja se é a melhor opção de mercado ou se existem
outras implementações melhores para extração dos chunks...
lembre-se, clareza e responsabilidade única são chave desse projeto, bem como uma camada de persistência bem abstraída para lidar com os bancos relacionais ou não relacionais.
validar qual melhor formato de trabalho, se mesclar typeORM com MongoDB ou utilizar libs tipo prisma para lidar com múltiplos bancos...
documente bem cada passo, para que seja um produto de fácil entendimento.
aproveite ao máximo a estrutura existente, mas exclua ou modifique o que julgar necessário.